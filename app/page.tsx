// Placeholder. Phase 1 is the framework move only -- no behaviour changes, so
// the real glance view lands in Phase 3 once the signals exist.
export default function Home() {
  return <p style={{ fontFamily: 'system-ui', padding: '2rem' }}>Nothing here yet.</p>
}
